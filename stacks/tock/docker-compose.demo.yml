version: '3'

services:

  # DEMO
  demo_api_webhook:
    image: tock/bot_api_webhook:$TAG
    depends_on:
      - mongo
      - mongo2
      - mongo3
      - bot_api
    environment:
      - tock_env=integ
    ports:
      - "5087:8887"

  demo_node:
    build: samples/tock-bot-node
    ports:
      - "5085:8080"

  # tock-bot-samples:
  #   build: tock-bot-samples
  #   environment:
  #     - tock_env=integ
  #   ports:
  #     - "5089:8887"
